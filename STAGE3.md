create 3 subagents that will do the code review of the solid-reviewer.agent.md
each subagent will run with different model:
1. gemini
2. claude
3. gpt

the solid-reviwer agent should be the orchastractor will call them as subagent in paraller,
the ecxpected output is to mark all the gaps and which model found each gap