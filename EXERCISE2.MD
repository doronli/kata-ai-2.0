# 🕵️‍♂️ Code Kata: "The Code Police"

**נושא:** בניית Agents & Skills באמצעות AI Primitives ב-VS Code.  
**קהל יעד:** Senior Full Stack Developers.  
**משך זמן:** 90-120 דקות 
**טכנולוגיה:** Node.js, GitHub Copilot, VS Code.

---

## 🎯 מטרות התרגיל (The "Wow" Effect)
1. **הבנה פרקטית:** יצירת "מוח" (Agent) ו"ידיים" (Skill) מאפס.
2. **שינוי תפיסתי:** הבנה כיצד להפוך את ה-LLM מכלי שיחה פסיבי לכלי עבודה אקטיבי ש"מריץ" קוד.
3. **יכולת יישום:** צמצום פערים טכנולוגיים בשימוש ב-AI Primitives בסביבה ארגונית מאובטחת.

---

## 🕒 לו"ז "חקירה משטרתית"

| זמן | שלב | תוכן Hands-on | דגש תיאורטי (בסיכום השלב) |
| :--- | :--- | :--- | :--- |
| **15 דק'** | **פתיחת אירוע** | התקנת ה-Repo וסקירת ה-Bad Code | הבדל בין Agent ל-Skill |
| **30 דק'** | **גיוס הבלש** | יצירת ה-Agent ב-`.github/agents` | מודל **S.P.E.C** לכתיבת פרומפטים |
| **45 דק'** | **ציוד טכנולוגי** | כתיבת ה-Skill הראשון ב-Node.js | Tool Definition & Schema |
| **20 דק'** | **חקירה באזהרה** | חיבור ה-Agent ל-Skill והרצה | Function Calling & Context |
| **10 דק'** | **מסדר זיהוי** | הדגמות וסיכום "ערך למחר בבוקר" | Personal Workflow Automation |

---

## 🛠️ מבנה ה-Repo (Boilerplate)
הקבצים שיהיו מוכנים למפתחים מראש:
* `src/crime-scene/`: קבצי קוד מלאים ב"עבירות" (Anti-patterns).
* `.github/agents/template.json`: מבנה JSON ריק להגדרת ה-Agent.
* `scripts/test-skill.js`: כלי עזר להרצת ה-Skill בטרמינל ללא ה-Chat.

---

## 📝 עקרונות ה-Prompting (מודל S.P.E.C)
כדי שהמפתחים יקבלו תוצאות איכותיות, נלמד אותם להגדיר:
* **S (Status/Role):** מי ה-Agent? (למשל: חוקר משטרתי קשוח).
* **P (Precise Task):** מה המשימה המדויקת שלו?
* **E (Examples/Tone):** איך הוא צריך לדבר? (ציני, קצר, רשמי).
* **C (Constraints):** מה אסור לו לעשות?

---

## 💡 דוגמה לתרחיש ה-Wow
המפתח פונה ל-Chat ב-VS Code:
> *"@inspector-police תבדוק את הקובץ `auth.js`"*

**התוצאה:**
ה-Agent מזהה שהמשתמש ביקש בדיקה -> מפעיל את ה-Skill שכתב המפתח -> הסקריפט מוצא `hardcoded-secret` -> ה-Agent עונה:
> *"נראה שתפסתי אותך על חם! מצאתי Secret חשוף בשורה 42. זה יעלה לך ב-50 נקודות ב-Code Review הבא."*

---

## 🔒 דגשי אבטחה (Enterprise Ready)
* **לוקאלי בלבד:** הכל רץ על המכונה של המפתח בתוך ה-Context של VS Code.
* **אין צורך ב-API Keys:** שימוש ב-Copilot המאושר ארגונית.
* **Node.js Core:** ה-Skills יתבססו על ספריות הליבה (fs, path) למניעת חסימות רשת.

---

> **הערה למנחה:** התרגיל נועד להיות חווייתי. עודד את המפתחים להפוך את ה-Agent שלהם לכמה שיותר "אישיותי" ומצחיק – זה מה שיגרום להם לזכור את העקרונות הטכנולוגיים שמאחורי הקלעים.